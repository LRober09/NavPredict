const url = 'http://localhost:8000';
const usersUrl = url + '/users';
const loginUrl = url + '/login';
const logoutUrl = url + '/logout';

const productsUrl = url + '/products';

export {usersUrl, loginUrl, logoutUrl, productsUrl};